Install react 

create exportable json component 
finish/src/titles.json
finish/src/lib.js

finish/src/index.js

Make sure to tell webpack 
{
    test: /\.json$/,
    exclude: /(node_modules)/,
    loader: "json-loader"
}